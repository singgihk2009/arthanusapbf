import { Head, Link, router } from '@inertiajs/react';import AppLayout from '@/Layouts/AppLayout';
export default function Page({ salesOrder }) { return <AppLayout><Head title='Sales Order'/><div className='p-6 space-y-3'><h1 className='text-xl font-semibold'>{salesOrder.number}</h1><div>Status: {salesOrder.status}</div><div>Customer: {salesOrder.customer?.customer_name}</div><table className='w-full border text-sm'><thead><tr><th>Item</th><th>Qty</th><th>Total</th></tr></thead><tbody>{salesOrder.lines?.map(l=><tr key={l.id}><td>{l.item?.name}</td><td>{l.qty_sold}</td><td>{l.line_total}</td></tr>)}</tbody></table><div className='flex gap-2'>{salesOrder.can_edit && <Link href={route('apps.sales-orders.edit', salesOrder.id)} className='border px-3 py-1'>Edit Draft</Link>}{salesOrder.can_submit && <button className='border px-3 py-1' onClick={()=>router.post(route('apps.sales-orders.submit', salesOrder.id))}>Submit</button>}{salesOrder.can_approve && <button className='border px-3 py-1' onClick={()=>router.post(route('apps.sales-orders.approve', salesOrder.id))}>Approve</button>}<Link href={route('apps.customers.show', salesOrder.customer_id)} className='border px-3 py-1'>Back to Customer</Link></div></div></AppLayout>; }
