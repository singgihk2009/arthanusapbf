import { Head, Link, router } from '@inertiajs/react';
import AppLayout from '@/Layouts/AppLayout';
export default function Page({ salesOrders, filters }) { return <AppLayout><Head title='Sales Orders' /><div className='p-6 space-y-4'><h1 className='text-xl font-semibold'>Sales Orders</h1><div className='rounded border bg-white p-3'><input className='border rounded px-2 py-1 text-sm' defaultValue={filters?.search || ''} placeholder='Search number' onKeyDown={(e)=>e.key==='Enter'&&router.get(route('apps.sales-orders.index'),{...filters,search:e.target.value},{preserveState:true})} /></div><table className='w-full text-sm bg-white border'><thead><tr><th>No</th><th>Customer</th><th>Status</th><th>Total</th></tr></thead><tbody>{salesOrders?.data?.map(so=><tr key={so.id}><td><Link className='text-blue-600' href={route('apps.sales-orders.show', so.id)}>{so.number}</Link></td><td>{so.customer?.customer_name}</td><td>{so.status}</td><td>{Number(so.grand_total).toLocaleString()}</td></tr>)}</tbody></table></div></AppLayout>; }
