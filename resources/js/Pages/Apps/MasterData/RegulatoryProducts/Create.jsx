import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout'; import { useForm } from '@inertiajs/react';
export default function Create({auth,sources}){const {data,setData,post}=useForm({source_id:'',nie:'',product_name_source:''}); return <AuthenticatedLayout user={auth.user}><div className='p-6'><h1>Create Regulatory Product</h1><form onSubmit={e=>{e.preventDefault();post('/apps/master-data/regulatory-products')}}><select onChange={e=>setData('source_id',e.target.value)}>{sources.map(s=><option key={s.id} value={s.id}>{s.source_name}</option>)}</select><input placeholder='NIE' onChange={e=>setData('nie',e.target.value)}/><input placeholder='Product Name' onChange={e=>setData('product_name_source',e.target.value)}/><button>Save</button></form></div></AuthenticatedLayout>}
